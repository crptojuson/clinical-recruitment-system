# 数据库配置（SQLite，无需额外配置）
# 数据库文件将自动创建在 backend/database.sqlite

# JWT配置
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=30d

# 服务器配置
PORT=3000
NODE_ENV=development

# 前端URL（用于CORS）
FRONTEND_URL=http://localhost:5173 